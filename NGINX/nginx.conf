# user nginx;
# worker_processes auto;
# pid /run/nginx.pid;

# events {
#     worker_connections 1024;
# }

# http {
server {
    listen 80;
    server_name localhost;
    root /var/www/html;
    index index.html;
}
# }
